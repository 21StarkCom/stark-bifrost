---
name: demo-command
description: Demo command exercised only by the claude adapter seed-bundle golden fixture.
argument-hint: '[--flag VALUE]'
model: sonnet
---
Demo command body. Frozen fixture content — edit only to deliberately update the
rendering golden (then bump bundle.yaml's version and run UPDATE_GOLDEN=1).
