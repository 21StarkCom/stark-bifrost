---
description: >-
  A skill whose frontmatter omits name; identity comes from the directory (the
  conventional stark-skills / Claude Code shape).
context: fork
---

This skill has no `name:` in frontmatter and a source-only `context` key.
